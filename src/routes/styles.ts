import {create} from '../utils';

export const styles = create({
  container: {
    flex: 1,
  },
  stack: {
    flex: 1,
    justifyContent: 'center',
  },
  drawerStyles: {
    flex: 1,
    width: '50%',
    backgroundColor: 'transparent',
  },
});
