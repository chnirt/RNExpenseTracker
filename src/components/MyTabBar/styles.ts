import {create} from '../../utils';

export const styles = create({
  container: {
    backgroundColor: 'red',
    flex: 1,
  },
  bottomTabContainer: {
    flexDirection: 'row',
    height: 64,
    alignItems: 'center',
  },
  bottomTabButton: {
    // flex: 1,
    // alignItems: 'center',
    backgroundColor: 'green',
    // backgroundColor: 'red',
  },
});
