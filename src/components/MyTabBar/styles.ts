import {create} from '../../utils';

export const styles = create({
  container: {
    backgroundColor: 'red',
    flex: 1,
  },
  bottomTabContainer: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  bottomTabButton: {
    height: 64,
    backgroundColor: 'green',
  },
});
